Installation Instructions

For a first-time build, run these commands to generate the makefiles and then
build the binaries.

    $ cmake .
    $ make
    $ sudo make install

You will need cmake (www.cmake.org) installed in order to build the software.
You'll like it.

---
